# Copy to .env (gitignored). Never commit real keys.
# Load before starting Claude Code:  set -a; source .env; set +a

# Google Maps Platform — Cloud Console; enable Places API (New),
# Routes API, and Geocoding API on the same key.
# (Note: the MCP uses Routes API, not the legacy Directions API.)
GOOGLE_MAPS_API_KEY=
